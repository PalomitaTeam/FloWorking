
package main

/*
func main() {

	myActivities :=  make([]Activity, 2)
	for i := 1; i <= 4; i++ {
		a := newActivity(
			fmt.Sprintf("Nombre_%d", i),
			60 + float32(i),
			fmt.Sprintf("Descripción_%d", i),
			newSubActivity(fmt.Sprintf("Sub_%d", i)),
		)
		myActivities = append(myActivities, a)
	}


	fmt.Print(myActivities)
}
*/
/**
	**Ojo al detalle**
	MiPropiedadPublica string
    miPropiedadPrivada string
*/

